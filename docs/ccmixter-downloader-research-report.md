# ccMixter Stem-Downloader für Secret Mixter

## Kurzfazit

Der richtige Hebel ist hier nicht noch ein bisschen besseres Klicken, sondern ein sauberer Produktwechsel: **API-first für Metadaten, HTML/Pagelogik als Ergänzung, Electron als Hauptplattform, Browser nur als abgespeckte Option**. Dafür sprechen drei Dinge. Erstens hat ccMixter eine dokumentierte Query-API mit Filtern wie `user`, `tags`, `lic` und `sources`, mehreren Ausgabeformaten einschließlich `json` sowie Datenansichten wie `info` und `files`. Zweitens zeigen reale Upload-Seiten, dass Stems in ccMixter sehr unterschiedlich publiziert werden: als ZIP-Archiv mit sichtbarer Inhaltsliste, als separate Stem-Uploads mit gleichem Songnamen, oder als zusätzliche Dateien am Haupt-Upload. Drittens gibt es bereits Community-Tools für reinen Batch-Download nach Artist oder Tag, aber nicht für den stem-spezifischen Workflow mit Gruppierung, Umbenennung, Zielordnern und automatischer Entpackung. Genau diese Lücke sollte dein neuer Downloader schließen. citeturn10view0turn7view1turn7view2turn12view2turn12view3turn16view0turn17view0turn17view1turn28view0

Wenn man es brutal ehrlich formuliert: **Ein „Rechtsklick-und-speichern“-Workflow ist kein Workflow, sondern Strafe.** Für deinen Anwendungsfall ist ein Zielbild wie dieses realistisch und sinnvoll: Der Nutzer fügt einen ccMixter-Artist- oder Upload-Link ein, wählt einmalig ein Zielverzeichnis, sieht danach automatisch erkannte Songgruppen mit BPM, Lizenz, Pakettyp und Dateiliste, und startet dann einen Sammeldownload, der die Dateien nach `[Artistname]/[Songname (x bpm)]/[Dateiname].[ext]` ablegt, ZIPs optional automatisch entpackt und zusätzlich eine Attribution-Datei pro Song mitschreibt. Diese Empfehlung passt sowohl zu den ccMixter-Seitenstrukturen als auch zu den Möglichkeiten von Electron und dem Dateisystem moderner Browser. citeturn20view0turn20view1turn20view2turn27view2turn6view2

## Was ccMixter technisch heute hergibt

Die wichtigste gute Nachricht zuerst: ccMixter hat keine komplett undokumentierte Blackbox, sondern eine **offizielle Query API 2.0**. Die Doku beschreibt den Basis-Endpunkt `/api/query`, Ausgabeformate wie `html`, `rss`, `xml`, `js`, `json` und `csv`, Filter wie `user`, `tags`, `lic`, `playlist`, `remixes` und `sources`, sowie Datenansichten wie `info` und `files`. Für dein Projekt ist das Gold wert, weil du damit Artist-Scans, Suchtreffer, Quellenbeziehungen und strukturierte Upload-Metadaten **API-basiert** holen kannst, statt alles mühsam aus HTML zu erraten. Außerdem nennt die Doku die Standard-Limits für Query-Kontexte im Bereich von typischerweise 100–200 Ergebnissen, was direkt auf Paging und Caching als Pflichtbestandteile hindeutet. citeturn10view0turn7view0turn7view1turn7view2

Gleichzeitig reicht die API **allein** nicht als einzige Wahrheit. Die eigentlichen Upload-Seiten zeigen Informationen, die für deinen Downloader operativ sehr wertvoll sind: BPM steht sichtbar auf der Seite, Tags wie `zip`, `flac`, `preview`, `multiple_formats` oder `archive` helfen bei der Dateiauswahl, und bei ZIP-basierten Uploads listet ccMixter sogar oft die Inhalte des Archivs vorab auf. Das sieht man zum Beispiel bei **Quarter-Inch Jack (Source)** und **If You're Not There With Me! [STEMS]**, wo die ZIP-Inhalte inklusive Dateinamen direkt auf der Seite genannt werden. **Under the Stars** zeigt sogar mehrere Stem-ZIPs mit FLAC-Inhalten in getrennten Paketen, und **Okay for Sound 145** dokumentiert ausdrücklich, dass Preview-Dateien und FLAC-Stems parallel vorliegen. Für einen robusten Downloader heißt das: **Metadaten aus der API, Anreicherungen und Dateilogik aus den Upload-Seiten**. citeturn12view2turn12view3turn34view0turn34view1turn34view2

Wichtig ist auch: ccMixter veröffentlicht Stems **nicht auf genau eine Art**. In einem Hilfethread wird erklärt, dass „Adding additional files to the main file“ die vorgesehene Methode für mehrere Dateien ist und deshalb „Manage Files“ im Submission-Formular existiert. Gleichzeitig zeigen reale Uploads wie **Boxcar heading West**, dass ein Haupttrack in der Beschreibung auf separate Sibling-Uploads wie **(pells)** oder **(instrumental stems)** verlinken kann. Das bedeutet praktisch: Dein Downloader muss **beides** beherrschen – zusätzliche Dateien an einem Upload und separat veröffentlichte Stem-Seiten, die über Titelähnlichkeit, BPM und Verlinkungen zusammengehören. Wer hier nur „Download-Links auf der aktuellen Seite“ einsammelt, baut wieder ein Tool, das halb funktioniert. Also exakt das, was du loswerden willst. citeturn36view0turn16view0turn17view0turn17view1

## Warum Electron die bessere Hauptplattform ist

Ein reiner Browser-Client ist technisch machbar, aber für einen produktionsreifen Stem-Downloader **nicht die beste Primärlösung**. Moderne Browser können mit der File System API durchaus Verzeichnisse auswählen, Unterordner anlegen und Dateien schreiben. `showDirectoryPicker()` liefert ein Directory-Handle, `getDirectoryHandle(..., { create: true })` kann Unterordner anlegen, und `createWritable()` erlaubt das Schreiben auf Disk. Das ist die gute Nachricht. Die schlechte ist: `showDirectoryPicker()` ist laut MDN **nicht Baseline**, hat also keine breite Browser-Abdeckung, ist als **experimental** markiert, funktioniert nur in **secure contexts (HTTPS)** und verlangt **user activation**, also eine echte Benutzeraktion. Mit anderen Worten: als Chromium-zentrierte Web-App okay, als zuverlässige Cross-Browser-Lösung eher Wunschdenken. citeturn20view0turn37view0turn30view0turn30view2

Electron löst genau die Punkte, die dich bei diesem Projekt wirklich nerven werden: gezielte Pfadsteuerung, automatische Zielordner, Fortschritt, Retry, Resume und kontrollierter Zugriff aufs lokale Dateisystem. Die `DownloadItem`-API erlaubt in `will-download` ein `setSavePath(path)`; existiert der Zielpfad noch nicht, versucht Electron die Verzeichnisse rekursiv anzulegen, und ohne gesetzten Pfad öffnet Electron sonst typischerweise einen Save-Dialog. Zusätzlich gibt es `updated`- und `done`-Events, Fortschrittswerte wie `getReceivedBytes()`, `getTotalBytes()` und `getPercentComplete()`, sowie `resume()` und `canResume()` für fortsetzbare Downloads. Genau das ist der Unterschied zwischen „geht irgendwie“ und „funktioniert wie ein richtiges Werkzeug“. citeturn35view0turn35view1turn35view2turn35view3turn35view4

Auch sicherheitstechnisch ist Electron beherrschbar, solange man nicht schlampig wird. Die offizielle Doku empfiehlt Context Isolation, die seit Electron 12 standardmäßig aktiviert ist, sowie schmale, kontrollierte APIs über `contextBridge` und Preload-Skripte statt einem Renderer mit vollem Node-Zugriff. Genau das brauchst du hier: Renderer für UI, Main Process für Netzwerk/Dateisystem, Preload als schmale Brücke. Außerdem sagt die Security-Doku ausdrücklich, dass externe Inhalte nur über **HTTPS** geladen werden sollen. Daraus folgt meine klare Empfehlung: **Electron-first** für die Haupt-App, optional eine **Browser-Light-Version** nur für Chromium-basierte Umgebungen und einfache Fälle. citeturn29view0turn29view1turn29view2turn20view3

## Welche Produktlogik wirklich robust wäre

Die Eingangsebene sollte maximal tolerant sein. Der Nutzer sollte einen **Artist-Link**, einen **Upload-Link** oder notfalls nur einen **Upload-ID/Artistnamen** eingeben können. Das passt zur ccMixter-Logik: Die Query API kann nach `user` filtern, und ein bestehendes Community-Tool nutzt bereits genau solche Eingaben – Artist, Tag, URL oder Track-ID – für Batch-Abfragen und Downloads. Technisch sinnvoll wäre daher: Alles zuerst auf eine interne Normalform bringen, also auf **artist login + optional upload id**. Danach lässt sich der Rest sauber systematisch auflösen. citeturn10view0turn28view0

Die eigentliche Kunst liegt in der **Gruppierung zu Songs**. Reale ccMixter-Titel belegen, dass ein Downloader Suffixe und Stem-Marker erkennen muss: Beispiele sind **(Source)**, **(pells)**, **(instrumental stems)** und **[STEMS]**. Daraus würde ich eine Kanonisierung ableiten: Der Ordnername basiert auf einem **bereinigten Songtitel**, bei dem solche Funktionssuffixe entfernt werden, während die eigentlichen Stem-Dateien ihren ausgewählten, bereinigten Namen behalten. Für die Gruppierung selbst würde ich mehrere Signale kombinieren: gleicher Artist, gleicher normalisierter Songroot, gleicher oder sehr ähnlicher BPM-Wert, Querverweise in der Beschreibung, zeitliche Nähe der Uploads und – wo verfügbar – Quellenbeziehungen aus der API. Wo das Ergebnis nicht eindeutig ist, sollte die UI keine Magie vortäuschen, sondern eine kurze Bestätigungsstufe zeigen. Das ist nicht elegant im Marketing-Sprech, aber solide in der Praxis. citeturn12view2turn16view0turn17view0turn17view1turn34view0

Für die Dateiauswahl würde ich **nicht blind jeden Download nehmen**, sondern einen kleinen Resolver bauen. Die Seiten selbst liefern dafür gute Hinweise: Tags wie `flac`, `zip`, `archive`, `preview` und `multiple_formats` zeigen, dass Uploads aus Vorschau- und Quellformaten gemischt bestehen können. **Okay for Sound 145** erklärt das explizit: Preview-Files sind mono, FLAC-Dateien stereo. **Under the Stars** zeigt mehrere Stem-ZIPs mit FLAC-Dateien. Daraus folgt eine saubere Produktregel: Standardmäßig „**höchste verfügbare Quellqualität**“, aber mit einer sichtbaren Option „**Vorschauen mitnehmen**“ oder „**nur verlustfreie Quellen**“. Das verhindert, dass der Downloader zwar brav alles speichert, aber die falschen Dateien einsammelt. citeturn34view1turn34view2

Die Ausgabestruktur, die du genannt hast, ist genau richtig und sollte intern strikt erzwungen werden: Artistordner, darunter Songordner mit BPM, darunter sauber benannte Dateien. Dabei ist Dateinamen-Sanitizing keine Kür, sondern Pflicht. Die Microsoft-Doku listet auf Windows die verbotenen Zeichen `< > : " / \ | ? *`, reservierte Namen wie `CON`, `PRN`, `AUX`, `NUL`, `COM1` bis `COM9` und `LPT1` bis `LPT9`, sowie das Problem von Namen mit abschließendem Punkt oder Leerzeichen. Electron/Node liefert dir mit `node:path` die plattformübergreifende Pfadverarbeitung; optional kann ein Paket wie `sanitize-filename` die praktische Bereinigung vereinfachen. Die sinnvolle Regel ist: **anzeigen, wie der Nutzer es versteht; speichern, wie das Dateisystem es akzeptiert**. citeturn25view1turn19search3turn19search7

Weil ccMixter-Nutzung an Attribution hängt, würde ich pro Songordner zusätzlich maschinell erzeugte Begleitdateien schreiben, zum Beispiel `attribution.txt`, `license.txt` und `source.json`. Das ist kein Luxus, sondern clevere Schadensvermeidung. ccMixter selbst erklärt, dass Tracks je nach Lizenz unter **CC BY** kommerziell nutzbar oder unter **CC BY-NC** nur für persönliche Nutzung freigegeben sind und dass Attribution zwingend dazugehört. Die Terms verlangen ebenfalls, dass Tracks in der geforderten Weise attribuiert und Lizenz-/Hinweistexte nicht entfernt oder verändert werden. Ein Downloader, der die Musik organisiert, aber die Credits wegwirft, macht nur die halbe Arbeit. citeturn27view2turn27view3turn6view2

Ein vernünftiges Zielergebnis sähe so aus:

```text
Artistname/
  Songname (96 bpm)/
    BASS.flac
    FLUTE (Lukas Engelke).flac
    GUITAR.flac
    VOCALS (Wiseman).flac
    attribution.txt
    license.txt
    source.json
```

## Wie die App für den Nutzer aussehen sollte

Die UI sollte radikal einfach bleiben. Ein guter Startbildschirm braucht praktisch nur drei Dinge: ein Eingabefeld für ccMixter-Link oder Artistnamen, einen Button zur Auswahl des Zielverzeichnisses und einen Scan-Button. Danach folgt keine technische Rohdatenwand, sondern eine **Review-Ansicht**, in der die App automatisch erkannte Songgruppen anzeigt: Titel, Artist, BPM, Lizenz, Pakettyp wie „ZIP“, „separate Stem-Seiten“ oder „zusätzliche Dateien“, plus die vorgeschlagene Zielstruktur. Diese Informationen sind auf ccMixter real vorhanden oder ableitbar, etwa über BPM-Felder, Lizenzhinweise, ZIP-Inhaltslisten und dokumentierte API-Filter. citeturn12view2turn12view3turn34view0turn34view1turn27view2turn10view0

Die beste UX ist hier nicht maximal „smart“, sondern **smart mit eingebautem Misstrauen gegen Ausreißer**. Das heißt konkret: automatische Vorschläge, aber sichtbare Umbenennungsvorschau; automatische Gruppierung, aber manuelle Zusammenführen/Trennen-Funktion; Qualitätsauswahl per Schalter; und eine Option „nur erkannte Stems“ versus „alle zugehörigen Quellpakete“. Gerade weil ccMixter sowohl Stem-ZIPs als auch separate Sibling-Uploads und Multiple-File-Uploads zulässt, muss die App dem Nutzer im Zweifel zeigen, **warum** etwas zu einem Song gehört. Das spart am Ende mehr Zeit als jede künstlich vereinfachte Blackbox. citeturn36view0turn16view0turn17view0turn17view1turn34view0turn34view1

Sehr sinnvoll wäre außerdem ein **Dry Run**. Also erst analysieren, gruppieren, umbenennen und Zielpfade simulieren, dann erst herunterladen. Dazu kommen ein Fortschrittsbildschirm mit Download-Fortschritt pro Datei und pro Song, Retry bei Fehlern und Resume, sofern der Server Range Requests plus `ETag` und `Last-Modified` unterstützt. Electron stellt dafür die nötigen Download-Hooks und Zustände bereits bereit. So merkt der Nutzer früh, ob die App aus „Artistname-Songname_[X].mp3“ wirklich „VOCALS.flac“ oder nur „mysteriöses_Paket_3.zip“ machen wird, bevor die Platte vollgeschrieben wird. citeturn35view0turn35view1turn35view4

## Wie ich die Architektur aufbauen würde

Ich würde die App in fünf logisch getrennte Schichten teilen. **Erstens** einen Metadata Resolver, der Query-API und Upload-Seiten zusammenführt. **Zweitens** einen Grouper, der Uploads zu Songs zusammenfasst. **Drittens** einen Download Manager, der Pfade setzt, Fortschritt meldet und Fehler behandelt. **Viertens** eine Extraction & Rename Pipeline, die ZIPs entpackt, Dateien auswählt, Namen bereinigt und Zielordner schreibt. **Fünftens** ein kleines lokales Catalog-Store, das Scan-Ergebnisse, manuelle Korrekturen und bereits heruntergeladene Assets cached. Diese Aufteilung passt sowohl zur ccMixter-Datenlage als auch zu Electrons Process-Modell mit Main, Renderer und Preload. citeturn10view0turn7view1turn29view0turn29view2turn20view2

Für die ZIP-Verarbeitung würde ich zweigleisig denken. In der **Electron-Hauptapp** ist eine stream-orientierte Lösung wie `unzipper` sinnvoll, weil sie Archive direkt in Verzeichnisse extrahieren kann und in der Praxis besser zu großen Paketen passt. Für eine **Browser-Fallback-Version** ist `JSZip` die pragmatischere Wahl; die Doku beschreibt das Laden vorhandener ZIPs per `loadAsync()` sowie das Lesen lokaler Dateien über `FileReader.readAsArrayBuffer(Blob)`. Falls du im Browser größere Downloads puffern willst, ist die Origin Private File System-Logik (OPFS) interessant, weil MDN sie explizit für lokale Sandboxes, große Download-Artefakte und die lokale Expansion von Asset-Paketen nennt. Für den Vollausbau bleibt Electron trotzdem das bessere Zuhause. citeturn21search4turn21search7turn21search0turn21search3turn37view0

Sicherheitstechnisch sollte der Renderer **nie** direkt ans Dateisystem dürfen. Der sichere Weg ist: Renderer sendet wohldefinierte Kommandos wie `scanArtist`, `downloadSongGroup`, `chooseTargetDir`, `previewRename`; der Main Process führt aus; der Preload gibt nur genau diese Funktionen über `contextBridge` frei. Die Electron-Doku warnt ausdrücklich davor, `ipcRenderer` oder breite Node-APIs ungefiltert in den Renderer zu kippen. Parallel dazu sollten externe Inhalte nur via HTTPS geladen werden. Für dein Projekt heißt das praktisch: **keine privilegierten BrowserViews mit fremdem Content**, sondern eigene UI und eigene Requests gegen dokumentierte ccMixter-Endpunkte. citeturn29view1turn29view2turn20view3

## Risiken, Fallstricke und die beste Reihenfolge

Die größten Risiken sind nicht exotisch, sondern banal und deshalb gefährlich: inkonsistente Titel, mehrdeutige Stem-Gruppen, Preview-Dateien statt echter Quellen, ZIPs mit verschachtelten Verzeichnissen, und Uploads, bei denen der eigentliche Stem-Hinweis nur im Beschreibungstext steckt. Genau diese Heterogenität sieht man in den Beispielen: ein Upload hat ein komplettes ZIP mit klar benannten FLAC-Stems, ein anderer verlinkt auf separate `(pells)`- und `(instrumental stems)`-Seiten, wieder ein anderer mischt Preview und FLAC und erklärt das nur im Text. Deshalb sollte deine App **bewusst heuristisch plus kontrollierbar** sein, nicht dogmatisch vollautomatisch. citeturn34view0turn34view1turn34view2turn16view0turn17view0turn17view1

Für den **MVP** würde ich mich auf Electron festlegen und drei Inputs akzeptieren: Artist-Link, Upload-Link, Upload-ID. Dann würde ich API-first scannen, zusätzlich die Upload-Seiten parsen, Stem-Gruppen heuristisch bilden, Zielpfade vorschlagen, automatisch herunterladen, ZIPs entpacken, Dateinamen sanitizen und Credits mitschreiben. Erst wenn das stabil läuft, würde ich Phase zwei angehen: Browser-Light-Version für Chromium, erweiterte Filter wie „nur FLAC“, persistente Kataloge, und vielleicht später sogar eine Spezialansicht „Secret Mixter Partner analysieren“. Technisch ist das alles machbar. Die Frage ist nur, ob man es sauber baut oder wieder in Richtung „semi-gut“ abbiegt. Meine Empfehlung ist glasklar: **sauber bauen, Electron zuerst, API-first, manuelle Korrekturpunkte einplanen.** citeturn20view2turn10view0turn7view1turn35view3turn27view2turn6view2